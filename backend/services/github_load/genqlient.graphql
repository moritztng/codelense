query getOrganizations(
  # @genqlient(omitempty: true)
  $cursor: String,
  $query: String!,
  ) {
  search(
    query: $query
    type: USER
    first: 100
    after: $cursor
  ) {
    userCount
    edges {
      node {
        ... on Organization {
          databaseId
          login
          name
          createdAt
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
