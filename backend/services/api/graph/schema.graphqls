# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Repositories {
  repositories: [Repository]!
}

type Repository {
  owner: String!
  name: String!
  stars: Int!
}

type Query {
  repositories(maxStars: Int!, first: Int!): Repositories!
}
